#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * _strncopy - Connect
 * @dest: First operand
 * @src: Second operand
 * @n: Third
 *
 * Return: The sum of the two parameters
 */

char *_strncpy(char *dest, char *src, int n);
{

	strncopy(dest, src, n);
	return (dest);
}
